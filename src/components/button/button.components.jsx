
const Button = ({children}) => (
    <button className="group h-12 w-12 border border-white rounded-full hover:border-indigo-500 transition-all"> 
                    {children}
    </button>
)

export default Button;